high performance Redis key value store exploring these topics:

threadlocking, mutexes, concurrency, rwlocks
wal connection, sstables, compaction, atomic lock free skip lists,
semaphores serialize, thread safe LRU, lsm tree, multiplexing, /proc, coalescing,
cache invalidation, memory paging, garbage collection, cpu scheduling, distributed systems
concensus raft/paxos, replication quorum leader follow, gossip protocol, sharding/partitioning hashing.
distributed transactions


bonus:
how can i debug and implement error handling?
setup a unit testing framework?


